apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    io.kompose.service: trivy-scanner-claim0
  name: trivy-scanner-claim0
spec:
  accessModes:
    - ReadOnlyMany
  resources:
    requests:
      storage: 100Mi
